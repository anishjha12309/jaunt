export function App() {
  return (
    <div className="min-h-screen bg-paper text-ink">
      <main className="mx-auto max-w-5xl px-6 py-24">
        <h1 className="text-page-title font-semibold">Conversation Inbox</h1>
        <p className="mt-2 text-body text-muted">Scaffold ready — phases build the rest.</p>
      </main>
    </div>
  )
}
